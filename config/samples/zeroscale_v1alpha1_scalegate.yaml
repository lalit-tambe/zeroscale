apiVersion: zeroscale.zeroscale.dev/v1alpha1
kind: ScaleGate
metadata:
  name: preview-api
  namespace: staging
spec:
  targetRef:
    kind: Deployment
    name: preview-api
  idleTimeoutSeconds: 60
  minReplicas: 0
  scaledReplicas: 1
  bufferTimeoutSeconds: 60
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: zeroscale
    app.kubernetes.io/managed-by: kustomize
  name: zeroscale-proxy-service
  namespace: system
spec:
  ports:
    - name: proxy
      port: 8082
      protocol: TCP
      targetPort: proxy
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: zeroscale
